# Web
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Database — MySQL is the relational store (aiomysql async / pymysql sync).
sqlalchemy>=2.0.36
aiomysql>=0.2.0
pymysql>=1.1.0
alembic>=1.14.0
# Postgres drivers kept for the future pgvector store (not used by the app yet).
asyncpg>=0.30.0
psycopg2-binary>=2.9.0

# Agent
google-adk>=1.0.0
litellm>=1.53.0
anthropic>=0.40.0

# MCP (version resolved by google-adk)
mcp>=1.5.0
fastmcp>=2.3.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
